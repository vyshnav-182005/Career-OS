{
  "name": "career-os",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "install:python": "venv\\Scripts\\pip install -r services\\resume-parser\\requirements.txt",
    "setup": "npm run install:frontend && python -m venv venv && npm run install:python"
  }
}
